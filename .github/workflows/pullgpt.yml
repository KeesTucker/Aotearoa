name: Example Workflow

on: [pull_request]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run PullGPT
        uses: KeesTucker/PullGPT@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
